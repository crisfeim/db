Aquí tienes un prompt estructurado para evaluar si el modelo es capaz de resolverlo correctamente en el primer intento, evitando las abstracciones innecesarias y yendo directo a la implementación idiomática del lenguaje:
```text
Implementa el siguiente algoritmo formal de concurrencia en Swift moderno:
S = {x, P1, P2, L}
x ∈ Z
Pi ∈ {Waiting, Finished} 
L ∈ {0,1}
S0 = {0, W, W, 0}
Increase i = (L = 0) /\ (Pi = W) /\ (L' = 1) /\ (x' = x + 1) /\ (Pi' = T)
REQUISITOS DE IMPLEMENTACIÓN:
1. No mapees las variables matemáticas (como L o el estado de Pi) uno a uno usando tipos primitivos (Int, Enum). Son abstracciones conceptuales para modelar la concurrencia.
2. Utiliza las primitivas nativas de concurrencia moderna de Swift (Actors y Tasks) para materializar el comportamiento. El actor debe actuar como el mecanismo de exclusión mutua implícito y las Tasks como los procesos concurrentes.
3. El código debe garantizar que, para N procesos simultáneos ejecutando 'Increase', el resultado final sea equivalente a S = {N, T, T, 0} de forma nativa tras la sincronización, asegurando la exclusión mutua y el fin del ciclo de vida de las tareas.
```
